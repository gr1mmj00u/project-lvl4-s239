extends /layouts/application.pug

block content
  h2 Basic info
  +formFor(f, { url: urlFor('user'), method: 'put' })
    +input(f, 'email', { label: 'Email' })
    +input(f, 'firstName', { label: 'First name'})
    +input(f, 'lastName', { label: 'Last name'})
    //- +input(f, 'password', { as: 'password', label: 'Old password'})
    //- +input(f, 'newPassword', { as: 'newPassword', label: 'New password'})
    .form-group
      +submit-success('Update')
      +a-btn-danger(urlFor('user'), { method: 'delete' }) Delete
