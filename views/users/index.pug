extends /layouts/application.pug

block content
  h1 List users
  table.table.table-striped.table-bordered.table-hover
    thead.thead-light
      tr
        th Full Name
        th Email
        th Created At
    tbody
      each user in users
        tr
          td= user.fullName()
          td= user.email
          td= user.createdAt
